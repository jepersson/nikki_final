<img src="../images/b.jpg">
<%= render :partial => 'personal_info' %>
<% if current_user == @user %>
	<%= link_to "edit", edit_user_path(@user) %>
<% end %>
<div id="stalk_form"><%= render 'stalk_form' if current_user != nil %></div>
